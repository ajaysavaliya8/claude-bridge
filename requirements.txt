# Runtime dependencies (mirror of pyproject [project].dependencies).
# For tests too, install:  pip install -e ".[dev]"
# Upper bounds cap fast-moving SDK majors; pin exact versions in a lockfile for
# reproducible production deploys.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
httpx>=0.27,<1
anthropic>=0.40,<1
mcp[cli]>=1.2,<2
python-multipart>=0.0.9,<1
